# WD Photo Replit source mirror

This directory mirrors the current Replit implementation, including the React album, protected API, GitHub synchronization contract, and owner schema. Runtime album edits are saved separately to the repository root as album.json and images/.
